node_modules
.next
out
build
.DS_Store
*.pem

.env
.env.local
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
.vercel
*.tsbuildinfo

# Briefing footage. Kept out of git — GitHub rejects blobs over 100MB, and a
# 100MB+ asset does not belong in a repo regardless. See README for how to
# supply it locally or point NEXT_PUBLIC_HANDOVER_VIDEO_URL at a hosted copy.
public/handover.mp4
*.mp4
