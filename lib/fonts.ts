import { Kalam } from "next/font/google";

/** The handwritten notes on the cream pages — the campaign creative's voice. */
export const hand = Kalam({ weight: ["400", "700"], subsets: ["latin"], display: "swap" });
